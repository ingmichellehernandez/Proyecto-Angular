export interface Product {
    id: string;
    image: string;
    title: string;
    price: number;
    description: string;
}
// Lo que debería tener un producto.
// el "export" indica que ya se puede usar en app.component.ts
